serverless-file-processing/
│── main.tf             # AWS provider setup
│── s3.tf               # S3 bucket & security settings
│── lambda.tf           # Lambda function & IAM roles
│── dynamodb.tf         # DynamoDB table & security
│── api_gateway.tf      # API Gateway setup & security
│── iam.tf              # IAM policies & security roles
│── variables.tf        # Define variables (e.g., region, bucket name)
│── outputs.tf          # Define Terraform output values
│── terraform.tfvars    # Store variable values
│── lambda_function.py  # Lambda function code
│── requirements.txt    # Dependencies for Lambda (if needed)
